server:
  port: 8761
spring:
  application:
    name: registry
  security:
    user:
      name: nick
      password: 123456

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/